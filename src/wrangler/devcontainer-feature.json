{
    "name": "Cloudflare Wrangler CLI (via npm)",
    "id": "wrangler",
    "version": "1.0.0",
    "description": "Install Cloudflare Wrangler CLI via NPM.",
    "options": {
        "version": {
            "type": "string",
            "default": "latest",
            "description": "Version to install."
        }
    },
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils"
    ]
}
